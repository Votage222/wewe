
                        import random
import string

def generate_username(length):
    """
    Generates a random username with the specified length.

    Parameters:
    - length: int
        The length of the username to be generated.

    Returns:
    - str:
        The randomly generated username.
    """

    # Generating a random username using lowercase letters and digits
    username = ''.join(random.choices(string.ascii_lowercase + string.digits, k=length))

    return username

def find_available_username(length, attempts):
    """
    Finds an available 3-letter username on Discord by generating random usernames and checking their availability.

    Parameters:
    - length: int
        The length of the username to be generated.
    - attempts: int
        The number of attempts to find an available username.

    Returns:
    - str or None:
        The available 3-letter username found, or None if no available username is found.
    """

    for _ in range(attempts):
        username = generate_username(length)

        # Check if the generated username is available on Discord
        # You can replace this check with your own logic to verify username availability on Discord
        if is_username_available_on_discord(username):
            return username

    return None

# Example usage:
length = 3
attempts = 100

available_username = find_available_username(length, attempts)

if available_username:
    print(f"An available 3-letter username on Discord: {available_username}")
else:
    print("No available 3-letter username found on Discord.")
                    
